# scientific-python-toolkit (satool)

Generate synthetic physics-like data, fit an exponential decay model (SciPy), and save a plot + CSV.
Proof-of-skill: packaging (pyproject), CLI, tests, scientific stack.

## Setup
```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -e ".[dev]"
EEOF
